# 🗺️ Product Roadmap

## 📝 计划中 (Planned)
> 待开发的需求池，按优先级排序

- [ ] 🔴 **[P0] C# 驱动通信与权限控制集成**
    - [ ] 封装驱动访问层：
        - [ ] `CreateFile("\\\\.\\SerialMonitor")`
        - [ ] `DeviceIoControl` 封装
        - [ ] IOCTL 常量与结构体对齐
    - [ ] 管理员权限检测：
        - [ ] 启动时检测当前进程是否为管理员
        - [ ] 非管理员时：
            - [ ] 禁用串口监控功能入口
            - [ ] UI 提示“需管理员权限”
        - [ ] （可选）提供一键“以管理员重启”
    - [ ] 驱动可用性检测：
        - [ ] 检测驱动是否已加载
        - [ ] 不可用时触发自动安装流程
    - [ ] 实现监控控制接口：
        - [ ] 启动监控（指定串口）
        - [ ] 停止监控
    - [ ] 实现数据读取机制：
        - [ ] 异步读取驱动数据
        - [ ] 解析 TX / RX 数据
        - [ ] 转换为统一数据模型

- [ ] 🔴 **[P0] 串口监控功能 UI 集成**
    - [ ] 在主界面新增“串口监控”功能入口：
        - [ ] 支持选择目标串口
        - [ ] 启动/停止监控按钮
    - [ ] 实时数据展示：
        - [ ] 数据流列表（时间 / 方向 / 内容）
        - [ ] 支持 Hex / Text 切换
    - [ ] 数据过滤功能：
        - [ ] 按方向（TX/RX）
        - [ ] 按关键字
    - [ ] 日志能力：
        - [ ] 保存监控数据
        - [ ] 导出文件
    - [ ] 状态提示：
        - [ ] 驱动状态（已加载/未加载）
        - [ ] 权限状态（管理员/非管理员）

- [ ] 🟡 **[P1] 性能与稳定性优化**
    - [ ] 大数据流处理：
        - [ ] 环形缓冲区
        - [ ] UI 分页或虚拟列表
    - [ ] 防止 UI 卡顿：
        - [ ] 后台线程处理数据
    - [ ] 异常处理：
        - [ ] 串口断开
        - [ ] 驱动异常
        - [ ] IOCTL 失败

- [ ] 🟡 **[P1] 拆分说明文件与文档体系优化**
    - [ ] 拆分文档：
        - [ ] `README.md`
        - [ ] `docs/architecture.md`
        - [ ] `docs/driver.md`
        - [ ] `docs/development.md`
        - [ ] `docs/deploy.md`
    - [ ] 明确架构说明：
        - [ ] C# ↔ 驱动通信流程
        - [ ] 数据流说明
    - [ ] 补充部署说明：
        - [ ] 驱动安装流程
        - [ ] 权限要求（管理员）
        - [ ] CI/CD（GitHub Actions）

- [ ] 🟢 **[P2] AI 辅助脚本开发子系统 (AI-Assisted Scripting Subsystem)**
    - [ ] **AI 服务商多云配置管理界面 (Multi-Provider Configuration Portal)**
        - [ ] **双栏式响应布局设计**：
            - 左侧检索栏（精简视窗）：集成多云提供商概览列表，动态呈现服务商友好名称（Friendly Name）、API 类型以及已激活的模型集群规模。
            - 右侧配置栏（宽幅主视窗）：采用“提供商元数据（上半部）”与“可用模型网格（下半部）”的纵向分流布局。
        - [ ] **云服务商核心属性配置规格**：
            - **服务商友好名称（Display Name）**：支持用户自定义，系统执行强唯一性校验（Unique Constraint）。
            - **唯一样式标识（Provider ID）**：基于友好名称动态生成符合 `Kebab-case` 的纯 ASCII 字符码。支持创建期（Creation Phase）人工干预微调，实例持久化后执行置灰锁定，作为运行时寻址的唯一句柄。
            - **服务类型（API Protocol）**：标准化预设接口协议，首期支持 `OpenAI Chat Compatible`、`Google Gemini` 与 `Anthropic Claude` 规范。
            - **基准端点（Base URL）**：支持网络代理、反向代理或企业专线内网网关地址自定义。
            - **凭据令牌（API Key）**：采用密码域掩码遮蔽输入，支持交互式明文/密文切换。
        - [ ] **模型资产清单控制规格（Model Asset List）**：
            - 列表视图定义：支持呈现模型底层标识符（Model ID）、能力图谱标签（Capability Tags，如 Code/Reasoning 等）、在线可用性探针（Ping Test）、详细信息配置卡（Detail Settings）及物理剔除机制（Delete）。
            - 供给模式双轨化：支持静态手动维护输入，或利用标准 `GET /v1/models` 接口自动发现并执行多选批量编入。
        - [ ] **配置生命周期与持久化保障**：
            - 依托应用本地存储层（LocalStorage/SQLite），与系统全局配置执行统一序列化落盘，确保进程生命周期重构（重启）时的配置无损恢复。
    - [ ] **基于代理框架（Agent Framework）的沉浸式 AI 脚本协同编辑器**
        - [ ] **微软 Agent Framework 架构集成**：
            - 深度整合微软 Agent 协同框架，将 AI 行为抽象化。
            - **多云多模适配抽象层**：设计统一的 `IAIChatService` 接口标准，将各异构 API 提供商的调用逻辑收敛于统一的多态实现。
        - [ ] **AI 代理认知域赋能（Context & Tools Access）**：
            - 将底层运行时的核心 API、内置对象及 Hook 生命周期接口作为 Schema 参数注入 AI 代理的上下文提示词中。
            - 集成脚本静态语法解析（AST-based Syntax Linter）与轻量级沙箱编译运行沙箱，作为 Agent 的外部评测工具链。
        - [ ] **工具调用（Tool Calling）安全边界限制**：
            - 严禁 AI 代理直接越权篡改主编辑器缓冲区（Editor Buffer）。
            - 封装并暴露高安全级别的精细化工具原语供 AI 代理调用：
                - `read_script`：获取当前编辑区完整代码上下文。
                - `edit_script`：提供定位、搜索，并通过块级替换（Replace-match 模式）进行局部的精准代码修补。
                - `write_script`：在用户确认或特定场景下，实现代码模板的全局覆盖写入。
        - [ ] **过程可观测性设计（Agent Observability & Tracing）**：
            - 构建 AI 代理生命周期运行轨迹的可视化流媒体响应：
                - 实时展示思考链路径（Thought Process Chain / COT）。
                - 结构化捕获并渲染外部工具的调用堆栈与入参（Tool Calls & Parameters）。
                - 渐进式流式渲染（Streaming Response）最终面向用户的业务层文字应答。

## 🚧 开发中 (In Progress)
> 当前正在进行的工作

- [ ] 🔴 **[P0] 驱动安装与生命周期管理（用户无感）**
    - [ ] 设计驱动安装策略：
        - [ ] 应用启动时自动检测驱动是否存在
        - [ ] 不存在则自动安装
    - [ ] 实现驱动安装逻辑（优先 pnputil）：
        - [ ] 调用 `pnputil /add-driver /install`
        - [ ] 或 fallback 使用 `sc create`
    - [ ] 实现驱动启动/停止控制：
        - [ ] 自动启动驱动服务
        - [ ] 应用退出时按需停止（可选）
    - [ ] 实现驱动卸载机制：
        - [ ] 提供内部接口（升级/异常时使用）
    - [ ] 处理异常情况：
        - [ ] 驱动安装失败提示
        - [ ] 驱动版本不匹配自动覆盖/更新
    - [ ] 安全与权限要求：
        - [ ] 所有驱动操作必须在管理员权限下执行

## ✅ 已完成 (Completed)
> 已验收通过的功能

- [x] 🔴 **[P0] 项目初始化 (Project Init)**
    - [x] 配置 `项目配置文件` (串口库, 状态管理框架, 本地数据库 等核心依赖)
    - [x] 建立 Feature-first 目录结构
    - [x] 配置 `代码检查规则` 规则

- [x] 🔴 **[P0] 串口配置 UI (Serial Config UI)**
    - [x] 串口选择下拉框（含刷新按钮）
    - [x] 波特率/数据位/停止位/校验位配置控件
    - [x] 打开/关闭串口按钮与状态指示
    - [x] 流控参数支持 (RTS/CTS, XON/XOFF, DTR/DSR 选项已添加)

- [x] 🔴 **[P0] 串口核心管理 (Serial Core) - 基础架构**
    - [x] 实现后台 执行环境 串口服务框架
    - [x] 串口列表自动扫描 API
    - [x] 串口打开/关闭状态管理与错误处理
    - [x] SerialPortConfig 数据模型 (波特率, 数据位, 停止位, 校验位)

- [x] 🔴 **[P0] 基础收发功能 (Basic I/O)**
    - [x] **接收区**: 实时显示数据流
    - [x] **接收区**: 支持 Hex (十六进制) 与 ASCII 文本模式切换
    - [x] **发送区**: 基础文本/Hex 发送框
    - [x] **发送区**: 发送按钮与发送状态反馈

- [x] 🔴 **[P0] 用户配置持久化 (User Settings Persistence)**
    - [x] 串口参数保存为 JSON 文件 (软件根目录下 `config.json`)
    - [x] 启动时自动加载上次的串口配置
    - [x] 支持保存: 串口名、波特率、数据位、停止位、校验位、流控

- [x] 🟡 **[P1] 发送辅助工具 (Send Helpers)**
    - [x] 定时循环发送功能 (自定义间隔 ms)
    - [x] 快捷发送选项: 自动追加 `\r\n` (回车换行)
    - [x] 快捷发送选项: 自动追加校验位 (Checksum/CRC16-MODBUS)

- [x] 🟡 **[P1] 视图交互与日志 (UI/UX & Logging)**
    - [x] **通信日志**: 接收数据实时/手动保存为文件 (.txt / .bin)
    - [x] **接收优化**: 接收数据自动换行开关
    - [x] **接收优化**: 显示数据时间戳 (精确到ms)
    - [x] **接收优化**: 暂停屏幕滚动 / 一键清空接收区
    - [x] **状态监控**: Rx / Tx 字节计数器与复位功能
    - [x] **多条指令列表**: 多条预设指令面板 (支持添加、编辑、删除、拖拽排序)
    - [x] **数据持久化**: 使用 LiteDB 保存用户的指令列表 
    - [x] **快速发送**: 双击或点击发送按钮即可发送预设指令

- [x] 🔴 **[P0] 多区域可折叠布局 (Multi-Zone Collapsible Layout)**
    - [x] **三区架构**: 引入 `多区域视图库`，实现 Left/Right/Bottom 三个可独立调整尺寸的面板区域。
    - [x] **Activity Bar**: 实现 VS Code 风格侧边导航栏，支持点击图标"展开/完全折叠"面板。
    - [x] **灵活配置**: 支持通过右键菜单将功能面板（如指令列表、波形）移动到不同区域 (L/R/B)。
    - [x] **状态锁定**: 确保串口配置面板固定在 Left 区域首位，不可移动。

- [x] 🟡 **[P1] 独立校验与摘要计算器 (Checksum/Hash Calculator)**
    - [x] **算法支持**: 涵盖 Checksum (Sum8/16), CRC8/16/32 (多种多项式/初值变体), XOR, 以及摘要算法 (MD5, SHA1/256)。
    - [x] **架构设计**: 采用 **策略模式 (Strategy Pattern)** 封装算法逻辑，确保未来扩展新算法时无需修改 UI 层，仅需添加具体策略类。
    - [x] **UI 实现**: 独立的计算工具窗口，支持 Hex/Ascii 输入预览及自动转换，支持将待发送帧设置到计算帧，以及将结果附加到待发送帧后。

- [x] 🟢 **[P2] CI/CD (Github Actions)**
    - [x] **触发条件**：推送符合版本号格式的标签时（如 `v1.0.0` 或 `V1.0.0`）
    - [x] **执行步骤**
        - 使用 `Release` 配置构建并发布项目
        - 打包免安装包：格式为 `.zip`，命名规范为 `FlexComDotnet_{Version}.zip`（`{Version}` 动态替换为实际 Tag 版本号）
        - 生成安装包：格式为 `.msix`，命名规范为 `FlexComDotnet_{Version}.msix`
        - 创建 GitHub Release：基于触发流水线的 Tag 创建发布
        - 填充 Release 说明：编写脚本自动从 `CHANGELOG.md` 提取对应版本的内容
        - 上传产物：将上述生成的 `.zip` 和 `.msix` 文件作为附件上传到 Release
    - [x] **CHANGELOG 规范与提取逻辑**
        - 格式要求：一级标题为版本号；二级标题固定为 `✨ 新功能`、`🚀 优化`、`🐛 Bug 修复`
        - 内容要求：二级标题下使用 Markdown 列表，必须以**用户视角**描述变更价值，而非技术实现细节
        - 提取校验：脚本根据 Tag 版本号匹配日志内容；若 `CHANGELOG.md` 中未找到对应版本号标题，**CI 流程必须失败**（熔断机制）

- [x] 🟡 **[P1] 智能自动回复系统 - 基础功能 (Smart Auto-Reply System)**
    - [x] **可扩展架构**: 建立统一的回复处理器接口（策略模式）。新增回复模式只需实现 `ReplyHandler` 接口并添加枚举关联。
    - [x] **配置管理**: 实现分层配置结构，统一序列化保存至 `config.json`。
        - **全局设置**: 总开关、全局回复延迟 (Delay)、当前激活模式。
        - **模式设置**: 每种模式拥有独立的配置模型 (`MatchReplyConfig`, `SequentialReplyConfig`)。
    - [x] **模式 1: 匹配回复 (Match & Reply)**:
        - 配置: 触发规则 (Hex/Ascii 包含匹配) -> 响应内容 (Hex/Ascii)。
        - 逻辑: 接收流中检测到特定特征码即触发回复。
        - UI: 规则列表（添加/编辑/删除/启用开关/拖拽排序）。
    - [x] **模式 2: 顺序回复 (Sequential Reply)**:
        - 配置: 预设的一组帧列表。
        - 逻辑: 每次收到数据后，按顺序发送列表中的下一帧，支持循环。
        - 交互: UI 指示当前回复进度（指针），支持手动重置或跳转到指定帧。
    - [x] **自动回复引擎**: 监听串口数据流，自动处理并发送回复，统计接收/回复次数。

- [x] 🟢 **[P2] 网络扩展 (Network Extension)**
    - [x] **抽象层重构**: 抽象出统一的 `IConnection` 接口，屏蔽串口与网络 Socket 的底层差异。
    - [x] **TCP 支持**: 实现 TCP Client (连接服务端) 和 TCP Server (本地监听) 模式。
    - [x] **UDP 支持**: 实现 UDP 单播与广播收发功能。

- [x] 🟢 **[P2] 自动更新 (Auto Update)**
    - [x] **版本检测逻辑**:
        - [x] 集成 `包信息工具` 获取本地版本。
        - [x] 接入 GitHub API (`GET /repos/silevilence/FlexComDotnet/releases/latest`) 获取远程版本。
        - [x] 实现 `语义版本比对工具` 版本号比对逻辑 (如 `1.0.1` > `1.0.0`)。
        - [x] 在设置页增加当前版本号显示。
    - [x] **下载管理器**:
        - [x] 实现 `HTTP客户端库` 下载流，支持下载进度回调。
        - [x] **UI 实现**: 在设置页增加"检查更新"按钮，以及弹窗显示 Release Note (更新日志) 和下载进度条。
    - [x] **安装包唤起**:
        - [x] 下载完成后使用 `文件打开工具` 唤起安装包。
        - [x] 优化退出逻辑：在安装包启动后，自动优雅关闭当前 应用程序 进程，避免文件占用锁死。

- [x] 🟢 **[P2] 通用脚本系统 (General Scripting System)**
    - [x] **核心架构与管理 (Core & Management)**
        - [x] **脚本引擎**: 集成 Lua 环境 (Lua运行时环境)，支持脚本加载、执行、停止。
        - [x] **脚本管理器**: 侧边栏/面板增加脚本管理页，支持文件的增删改查及简单语法高亮编辑。
        - [x] **API 桥接**: 暴露 `FCom` 全局对象，提供 `send`, `log`, `delay`, `crc16`, `crc32`, `checksum`, `getTimestamp` 等核心能力。
        - [x] **调试控制台**: 独立的脚本日志输出窗口，用于打印调试信息和显示运行状态。
    - [x] **Hook 挂载机制 (Hooking Mechanism)**
        - [x] **Pipeline Hook (数据流钩子)**: 允许将脚本挂载为"接收预处理器" (修改/解密 Rx 数据) 或"发送后处理器" (加封包/校验 Tx 数据)。
        - [x] **Reply Hook (应答钩子)**: 接入 P1 的自动回复系统，支持"脚本模式"，实现复杂的条件判断应答逻辑。
        - [x] **Task Hook (自动化任务)**: 支持手动触发脚本，用于执行一次性的发包序列或压力测试。

- [x] 🟢 **[P2] 脚本系统优化**
    - [x] **语法高亮**
        - Lua语法高亮
    - [x] **API提示与智能补全**
        - 输入时，可以出现智能提示补全相关语法或API
        - 补全时对相关方法有类型与说明
    - [x] **参考文档更新**
        - 编辑器放大后，可以打开API参考界面方便参考查看所有API

- [x] 🟢 **[P2] 通用帧协议解析引擎 (Universal Frame Parser)**
    - [x] **可扩展架构设计**:
        - 定义统一的 `IProtocolParser` 接口 (包含 `parse(字节数组 frame) -> ParsedFrame`)。
        - 采用**策略模式**管理协议库：新增协议仅需编写一个实现类 + 在枚举中注册，确保零耦合扩展。
    - [x] **通用解析器实现 (MVP)**:
        - 实现一个基于配置的默认解析器。
        - **字节提取**: 支持配置"第 M 到 N 字节"作为数据段，支持指定端序 (Little/Big Endian) 和数据类型 (Int/Float/Double)。
        - **位域解析 (Bit-field)**: 支持在一个字节内通过掩码 (Mask) 或位索引提取 Flag 标志位。
    - [x] **帧结构定义 UI**: 提供图形化界面配置帧头、帧尾、校验位位置及数据段提取规则，配置可保存为 JSON。

- [x] 🟢 **[P2] DL/T 645-2007 协议解析器实现 (DL/T 645-2007 Parser)**
    - [x] **数据流与完整帧提取 (Stream & Frame Extraction)**:
        - 基于 `TryExtractFrame` 实现粘包与半包处理，支持从连续的网络/串口字节流中截取完整数据帧。
        - 支持自动识别并过滤报文头部的 `FE` 唤醒前导码，精准定位报文起始符。
        - 根据报文内的长度域动态计算整帧长度，确保提取的帧结构完整。
    - [x] **帧合法性校验 (Frame Validation)**:
        - 基于 `Validate` 实现报文格式检查，验证起始符、结束符等固定标识。
        - 实现协议规定的算术累加校验和 (CS) 算法，保障通信数据的准确性与防篡改。
    - [x] **核心报文拆解与特殊编码转换 (Core Decoding & Rule Handling)**:
        - **地址解析**：支持小端序 (低字节在前) BCD 码的提取，还原 12 位通信表地址。
        - **控制码解析**：识别通信方向 (主/从站)、功能类型 (读数据、写数据等) 以及正常/异常应答标志。
        - **数据域处理**：实现该协议特有的数据域加减偏置算法（接收时统一执行 `-33H` 还原）以及 BCD 码到常规数值的转换。
    - [x] **数据标识字典与业务语义映射 (DI Mapping & Semantics)**:
        - 提取 4 字节数据标识 (DI)，建立 DI 与具体业务物理量（如：组合有功总电能、A相电压等）的映射字典。
        - 依据数据字典中定义的格式（如数据长度、小数位数），将还原后的数据动态格式化为对应精度的浮点数或字符串。
    - [x] **解析结果标准封装与异常翻译 (Result Packaging & Error Handling)**:
        - 将解析后的设备地址、功能指令、数据项名称、数值和单位等统一装载至 `ParsedFrame` 模型中返回。
        - 针对电表返回的异常应答报文，解析错误信息字（ERR），并翻译为具体的异常原因（如密码错、无此数据项等）。
    - [x] **UI修改 (User Interface)**
        - 在新建协议时，可以选择新建通用协议或是DL/T 645-2007协议
        - 根据选择的协议不同，协议的配置项也不同
            - 与通用协议不同，645协议不需要配置其它的帧头什么的，其它如表号、校验等字段自动提取（且不允许重名），用户可以配置对数据域部分的解析

- [x] 🟢 **[P2] 数据可视化 (Data Visualization)**
    - [x] **数据源绑定 (Data Binding)**:
        - 实现与"协议解析引擎"的联动。
        - **选择器 UI**: 允许用户从当前协议解析出的字段列表中，勾选需要作为 Y 轴数据的字段。
    - [x] **绘图引擎集成**: 引入 `图表库` 绘图库。
    - [x] **实时示波器**:
        - 实现多通道数据流的实时滚屏绘制。
        - 交互功能：支持波形暂停/继续、X/Y 轴缩放、十字游标测量。
        - 触摸提示：点击数据点显示通道名、数值、时间。
        - 通道管理：添加/删除通道，颜色配置，可见性切换。
        - 前置条件检查：连接状态、协议解析状态、通道匹配校验。
        - 数据导出：支持导出为 PNG 图片和 CSV 数据文件。

- [x] 🟢 **[P2] UI 调整（UI Adjust）**
    - [x] 左右下三个面板区采用类 VS Code 的标签式交互：将所有面板名称以标签形式常驻显示；点击标签展开对应面板，再次点击则折叠；同侧同时只能展开一个面板（例：右侧已展开面板1 → 点击面板2，右侧切换展示面板2；点击面板1，右侧全部折叠，仅显示所有面板标签）
    - [x] 移除现有的左侧统一控制栏（原用于统一管理左/右/下面板的显示与隐藏）
    - [x] 各面板支持在菜单中独立配置「可见性」：隐藏时，对应标签从面板区完全移除；重新设为可见时，不仅恢复标签显示，还应自动将该面板展开并置为激活状态
    - [x] 不影响现有功能，包括面板宽/高度拖拽调整与布局状态记忆
    - [x] 左侧与右侧的面板标签竖向排列，底部面板的标签横向排列

- [x] 🟢 **[P2] 日志整合**
    - [x] 日志统一收口
        - 所有功能模块（自动回复、脚本等）的日志统一输出至同一个日志面板
        - 日志条目需明确标注来源模块，以便区分
        - 移除各功能模块内原有的独立日志面板
    - [x] 覆盖信息（Info）、警告（Warning）、异常（Error）等完整日志等级的记录
    - [x] 以颜色区分不同日志等级，并为每个等级配置具有**形状辨识度**的 emoji 前缀（避免使用仅靠颜色区分的图形，如 🟢 🔴；应选用形状各异的图标，如 ✅ ⚠️ ❌ 💡 等）
    - [x] 日志本地持久化存储
        - 记录字段应包含：日期时间、日志等级、来源模块、日志内容及其他必要上下文信息
        - 界面仅展示本次程序启动后产生的日志
        - 日志文件按日期分文件存储，同一日期文件内需通过分隔标记区分程序的每次启动与关闭
    - [x] 日志面板支持多维度筛选，包括：日志等级、关键词搜索、来源模块、时间范围等

- [x] 🟢 **[P2] 界面修改与调试增强**
    - [x] 主界面左下角新增「设置」入口，点击唤出独立的设置窗口，需包含以下模块：
        - [x] 调试设置：提供全局开关，用于启用或停用「调试模式」
        - [x] 面板管理：将原位于主界面左下角的「面板管理」功能整体迁移至当前设置页内
        - [x] 日志目录：提供快捷按钮，点击可调用系统资源管理器打开日志保存的本地文件夹
        - [x] 关于信息：展示当前版本号、GitHub 仓库链接及开源许可声明（*注：「检查更新」功能仍保留在主界面，不迁入此页*）
    - [x] 调试模式功能规范（需在「设置」中开启后生效）：
        - [x] 快捷触发：在主界面按下 `F12` 键，可唤出独立的「调试工具」窗口
        - [x] 调试工具窗口界面与功能要求：
            - [x] Tab 页签式架构：为保证后续扩展性，界面需采用 Tab 布局。即使目前仅有单一功能，也需作为独立 Tab 展示，方便未来接入新调试模块
            - [x] 日志调试模块（首个 Tab）：
                - 支持用户选择日志的「来源（Source）」与「等级（Level）」
                - 提供日志内容输入框（默认预设文本为 "test log"，允许用户自定义修改）
                - 提供「发送日志」按钮，点击后根据设定的参数模拟生成并记录一条对应的日志

- [x] 🟢 **[P2] 自动回复与协议功能增加**
    - [x] 协议底层支持基于用户设定的各数据域（Data Field）值，动态组合/构建完整的数据帧（Data Frame）
    - [x] 协议管理界面新增「测试数据帧组合」模块（定位于现有的「测试解析」模块下方）
        - [x] 动态 UI 渲染：组合测试时的输入表单，需根据不同协议的定义及其包含的数据项结构进行动态生成与切换
        - [x] 协议列表交互优化：双击协议列表项即可直接进入「编辑状态」。在当前协议编辑期间，若触发切换操作（双击其他协议或点击新增）：若未修改过表单，则直接无缝切换；若表单已被修改（处于 Dirty 状态），则需强制弹出二次确认弹窗，询问用户“是否放弃当前修改”再进行跳转
    - [x] 自动回复功能新增「按协议回复」模式
        - [x] 提供协议选择器，允许用户指定回复协议并为该协议的具体数据项配置参数
        - [x] 动态表达式解析：数据项的输入框需支持插值语法与基础公式计算（参考 C# 的字符串插值规范，允许使用 `{}` 包裹并执行简单的计算代码或变量调用）
        - [x] 表单联动响应：与「测试数据帧组合」模块一致，此处的输入 UI 同样需根据所选定的协议动态渲染输入字段
        - [x] 配置持久化：用户的协议选择及各项输入数据，需作为完整的配置方案进行保存记录
        - [x] 多配置管理与互斥：支持创建并存储多套自动回复配置方案，但采用「单选互斥」机制（全局同一时间仅允许勾选并激活一套配置生效）

- [x] 🟢 **[P2] UI与配置优化**
    - [x] 自动回复配置重构（统一规则池）
        - [x] 界面与架构整合：废除原有的“模式单选”面板布局，将匹配规则、顺序帧、协议回复三种模式统一整合为「自动回复规则池」列表。列表中需明确展示各规则的“名称”与“类型”。
        - [x] 规则粒度细化：
            - 普通匹配规则、协议回复规则均作为独立的规则项记录。
            - 顺序回复重构：由原本只能配置全局单套规则，升级为支持将“特定帧组+是否循环”打包为独立的一条规则，允许配置多套顺序回复。
        - [x] 并发触发机制：取消全局单选限制，支持多选（Checkbox）激活多条跨类型规则。触发时需按照列表设定的优先级顺序依次计算执行（即一次触发动作允许生成并发送多条自动回复）。
    - [x] 脚本 Hook 配置优化
        - [x] 差异化持久化策略：将 Hook 引用的脚本配置数据持久化保存至本地配置文件。但出于安全与状态一致性考量，「启用状态」不参与持久化，每次启动软件时均默认重置为“未启用（Disabled）”状态。
        - [x] 依赖检测与级联清理：在执行脚本删除操作时增加引用校验。若目标脚本已被当前 Hook 规则绑定，需触发拦截告警询问用户；若用户确认强行删除，系统需同步解除/清空对应 Hook 中的脚本绑定记录（置空处理）。
    - [x] 彩色 Emoji 渲染与 IntelliSense 支持
        - [x] 引入 `Emoji.WPF` 渲染引擎：全局替换原有基于 Nerd Font 的单色内置 Emoji 方案，实现接收区及输入区的彩色原生渲染。
        - [x] 划定生效范围：除数据帧和纯数据输入文本框外，全局所有目标为“自由输入文本”的文本框均需接入此支持与补全功能。
        - [x] 实现 Emoji 智能补全（IntelliSense）机制：
            *   **前置依赖**：内置或构建标准 Emoji 及其 Shortcode（短码）的映射数据源。
            *   **动态触发与渲染**：监听文本输入，当探测到 `:` 且紧跟有效字符（字母/数字）时，在光标相对位置弹出跟随式的候选项浮窗。
            *   **实时检索与交互**：随输入动态过滤候选项（如输入 `:sm` 匹配 `:smile:`）。支持键盘（`↑/↓` 导航，`Enter/Tab` 确认）及鼠标（点击选中）操作流。
            *   **文本替换与防呆容错**：确认选择后，自动将光标前的短码（含 `:`）替换为对应的彩色 Emoji 符号并重置光标；若用户输入空格、标点符号或按下 `Esc` 键，则立即销毁浮窗，保留原有纯文本输入不进行强制干预。

- [x] 🟢 **[P2] 协议相关功能优化与扩展**
    - [x] 接收区（Rx）协议逆向解析联动
        - [x] 交互入口：支持在接收区选中特定的数据帧记录，通过左键或右键菜单快捷触发「协议解析」操作
        - [x] 独立视窗：弹出非模态浮窗（Non-modal Window），默认将选中的 Hex 数据带入输入区。用户指定协议后，系统自动反向解析（Parse）并结构化展示各数据域（Data Field）的明细
    - [x] 发送区（Tx）协议快捷组帧与回填
        - [x] 交互唤出：发送区功能栏新增「协议组帧」按钮，点击唤出非模态浮窗，并自动读取当前发送输入框的内容
        - [x] 双向解析映射：选择协议后，若带入的文本符合该协议结构，需支持反向抽取并自动填充至对应的数据项输入框中；反之，支持用户手动修改各数据项的值
        - [x] 回填策略：组帧完毕后，提供「覆盖当前内容（Overwrite）」和「追加至尾部（Append）」两种回填模式
        - [x] 模式强制纠偏：数据回写至发送区时，需强制校验并自动将发送区切换为「Hex 模式」，防止格式错乱
    - [x] 自动回复引擎深度集成协议机制
        - [x] 架构降维合并：废除独立的「脚本回复」顶级分类，将其底层逻辑与「顺序回复」、「匹配规则回复」完全融合
        - [x] 响应载荷动态构建：在配置上述两种规则的响应内容时，提供「纯文本手动输入」与「协议动态组帧」两种模式的切换开关
        - [x] 触发器扩展（协议级断言）：匹配规则新增“基于协议结构”的触发条件。系统将尝试用选中协议解析目标帧，以「解析成功/解析失败」作为布尔判定条件
        - [x] 细粒度字段断言（数据域级校验）：
            - 支持按需选取协议中的单个或多个数据项配置断言规则（多条件按 AND 逻辑运算；未配置的字段默认放行）
            - 支持丰富的关系运算符：`>`、`>=`、`<`、`<=`、`==` 及 `Hex包含`
            - 动态上下文提取：当规则匹配成功时，支持将解析出的数据项值作为变量，供响应内容的插值表达式（如 `{}`）引用
    - [x] Lua 脚本引擎 API 扩展与 IntelliSense 增强
        - [x] 协议对象 API 注入：
            - 新增检索接口：支持获取系统已注册的所有协议列表，支持按名称索引并实例化特定协议对象
            - 协议自省能力：协议对象需暴露内部结构，支持获取所有数据项定义及注释说明
            - 新增编解码核心方法：提供 `parse(protocol, frame)`（解码：返回字段集合或异常信息）与 `build(protocol, fields)`（编码：传入参数集合，输出合法帧或异常信息）
        - [x] 智能补全（IntelliSense）：脚本编辑器需支持协议名称、协议内数据项名称的上下文感知与自动代码提示补全
        - [x] 静态语法/依赖检查：脚本编辑态增加校验逻辑。若代码中引用了已被删除的协议或废弃的数据项名称，需在界面上予以明显的错误提示（如红色波浪线或 Error 提示）
    - [x] 协议管理依赖图谱与安全拦截
        - [x] 协议更新拦截（Dirty 校验）：修改协议并触发保存时，系统需检索当前是否有脚本正在引用此协议。若命中依赖，弹出拦截警告并提供三个流转分支：`强行覆盖保存`、`另存为新协议（克隆模式）`、`取消放弃`
        - [x] 协议删除拦截：执行删除协议操作时，检索脚本依赖图谱。若存在关联，弹出警告并要求用户二次确认：`强行删除`、`取消操作`

- [x] 🟢 **[P2] 协议解析库扩展（新增 Modbus-RTU 支持）**
    - [x] 协议（指令）类型拓扑结构更新
        - [x] 扩展新建入口：在「协议管理」新建协议时，扩展解析规则。在现有的「DL/T 645」和「自定义协议」基础之上，新增「Modbus-RTU」规则选项
        - [x] 明确定义边界：创建一个 Modbus-RTU 协议，实质上是定义一条具体的 Modbus 读/写指令（例如：读取指定寄存器的温湿度数据），并规划其需要提取或下发的数据项
    - [x] 核心字段定义与动态 UI 联动
        - [x] 基础帧头配置：针对 Modbus-RTU 指令，固定提供「从站地址（Slave ID）」与「功能码（Function Code）」等标准字段的输入/选择
        - [x] 基于功能码的智能表单渲染：数据项（Data Items）配置表单需监听「功能码」的变更，动态切换并重绘表单结构。例如：
            - 选择 `03` (读保持寄存器) 或 `04` 时：表单切换为配置「起始地址」和「读取数量」，并将预期的响应报文数据区映射为待提取的「业务数据项」（如：温度值、湿度值）
            - 选择 `06` (写单个寄存器) 时：表单切换为配置「寄存器地址」与「写入值」数据项
            - 选择 `10 Hex` (写多个寄存器) 时：表单展开支持「起始地址」、「寄存器数量」及对应的「多组写入值」数据项配置
    - [x] 底层引擎的自动化编解码与校验
        - [x] 组帧态（Tx / 自动回复构造）：当快捷组帧或触发自动回复时，底层引擎依据填入的数据项自动拼接报文主体，并**静默计算/追加正确的 `CRC-16/MODBUS` 校验码**，屏蔽用户对 CRC 的手动干预
        - [x] 解析态（Rx / 规则匹配）：当接收区解析或自动回复进行条件判断时，引擎强制优先执行 CRC 校验。校验通过后，严格按照该指令定义的功能码结构，剥离帧头帧尾，精准提取出用户定义的业务数据项集合
    - [x] Lua 脚本引擎架构一致性适配
        - [x] 无缝接入现有模型：在 Lua 脚本层面，Modbus-RTU 指令对象与已有的 645、自定义指令对象在调用方式上保持**绝对一致**（遵循统一的 `引擎 -> 协议(指令)对象 -> 数据项集合` 交互逻辑）
        - [x] 屏蔽底层协议细节：在脚本中调用此对象的 `build()` 方法组帧时，只需传入业务数据项，引擎自动补全帧头与 CRC；调用 `parse()` 方法时，引擎自动完成 CRC 校验并仅返回提取好的纯数据项集合（或在校验失败时返回异常），无需额外增加专门针对 Modbus 的独立 API

- [x] 🟡 **[P1] 拆分 Copilot 指导文件结构**
    - [x] 精简 `.github/copilot-instructions.md`，仅保留跨语言通用规范
    - [x] 新增 `.github/instructions/dotnet.md`：
        - [x] 迁移现有 C# / WPF / MVVM 相关规范
    - [x] 新增 `.github/instructions/rust.md`：
        - [x] 定义 Rust 编码规范（unsafe、错误处理、FFI 等）
    - [x] 新增 `.github/instructions/driver.md`：
        - [x] 定义驱动开发约束（IRP、性能、安全性等）
    - [x]（可选）在以下目录增加局部指导文件：
        - [x] `src/`：限定为 C# 规则
        - [x] `driver/`：限定为 Rust + 驱动规则
    - [x] 确保不同语言规范之间无冲突、不混用

- [x] 🔴 **[P0] 新增 Rust 串口监控驱动（基础能力）**
    - [x] 新建 `driver/serial-monitor/` Rust 驱动工程：
        - [x] 初始化 Cargo 项目（支持内核模式）
        - [x] 配置构建脚本（build.rs）对接 WDK
    - [x] 实现驱动基础框架：
        - [x] DriverEntry / 卸载逻辑
        - [x] 创建设备对象（`\Device\SerialMonitor`）
        - [x] 建立符号链接（`\DosDevices\SerialMonitor`）
    - [x] 实现过滤驱动核心能力：
        - [x] Attach 到串口设备（serial.sys / usbser.sys）
        - [x] 拦截 IRP：
            - [x] `IRP_MJ_READ`
            - [x] `IRP_MJ_WRITE`
            - [x] `IRP_MJ_DEVICE_CONTROL`
        - [x] 数据透传 + 捕获（不影响原通信）
    - [x] 实现用户态通信接口：
        - [x] 定义 IOCTL：
            - [x] 启动监控
            - [x] 停止监控
            - [x] 获取数据
        - [x] 定义共享数据结构（#[repr(C)]）
    - [x] 输出驱动产物：
        - [x] `.sys`
        - [x] （可选）`.inf`

- [x] 🟢 **[P2] 串口通信传输控制与自动响应机制优化**
    - [x] **串口帧定界与传输控制策略**
        - [x] **帧间隔超时判定（字节间延时）**：
            - 设定参数：允许配置以毫秒（ms）为单位的帧间隔时间。
            - 业务逻辑：接收数据流时，若相邻两字节的时间间隔小于设定阈值，则视为同一数据帧；若超过设定阈值，则自动进行分帧截断。
        - [x] **最大帧长度限制**：
            - 设定参数：允许配置最大单帧字节数（Byte）。
            - 业务逻辑：在帧间隔超时机制的基础上，若单帧累积长度达到或超过设定阈值，则强制进行物理分包分帧，以防止大缓冲区溢出。
        - [x] **运行期生命周期控制**：
            - 以上参数仅支持在“连接建立前”（离线状态）进行配置；连接建立后（在线状态），相关配置项强制置灰只读，确保通信会话期间参数的强一致性。
    - [x] **自动响应（Auto-Response）防抖与多帧决策过滤**
        - [x] **防抖延迟机制优化**：
            - 业务逻辑：将“回复延迟”复用为防抖时间窗口。在延迟计时器运行期间，若再次接收到新的数据帧，则立即重置并重新启动该计时器。
            - 预期效果：解决高频发包场景下的多重触发问题，由原有的“多次触发、多次回复”优化为“仅针对最后一次满足条件的帧进行单次响应”。
        - [x] **多帧联合决策判定（匹配规则扩展）**：
            - 对防抖延迟（时间窗口）内接收到的所有历史数据帧，引入多帧聚合匹配决策逻辑。
            - 提供全局匹配模式配置项（暂不开放单条规则定制），支持以下判定策略：
                - `AND`（全条件交集模式）：时间窗口内的所有数据帧必须全部触发匹配规则，方执行回复。
                - `OR`（任意条件并集模式）：时间窗口内只要有任意一帧触发匹配规则，即执行回复。
                - `LAST`（末帧判定模式）：仅对时间窗口内接收到的最后一帧进行规则校验，忽略历史帧。
                - `FIRST`（首帧判定模式）：仅对时间窗口内接收到的第一帧进行规则校验，忽略后续帧。
