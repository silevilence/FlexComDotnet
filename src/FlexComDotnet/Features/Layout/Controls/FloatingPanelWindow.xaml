<Window x:Class="FlexComDotnet.Features.Layout.Controls.FloatingPanelWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="浮动面板" 
        Height="400" 
        Width="300"
        MinHeight="200"
        MinWidth="200"
        WindowStyle="ToolWindow"
        ShowInTaskbar="False"
        ResizeMode="CanResizeWithGrip"
        AllowDrop="True"
        Closing="Window_Closing"
        LocationChanged="Window_LocationChanged"
        SizeChanged="Window_SizeChanged">
    
    <Window.Resources>
        <Style x:Key="DockButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="20"/>
            <Setter Property="Height" Value="20"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#666666"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="2,0"/>
            <Setter Property="ToolTip" Value="停靠到主窗口"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="2">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E0E0E0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- 标题栏（可拖动） -->
        <Border x:Name="TitleBar" 
                Grid.Row="0" 
                Background="#F3F3F3"
                BorderBrush="#E0E0E0"
                BorderThickness="0,0,0,1"
                Padding="8,4"
                MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock x:Name="TitleText"
                           Grid.Column="0"
                           Text="面板标题"
                           FontWeight="SemiBold"
                           FontSize="12"
                           VerticalAlignment="Center"
                           Foreground="#333333"/>
                
                <!-- 停靠按钮组 -->
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Style="{StaticResource DockButtonStyle}"
                            ToolTip="停靠到左侧"
                            Click="DockLeft_Click">
                        <TextBlock Text="◀" FontSize="10"/>
                    </Button>
                    <Button Style="{StaticResource DockButtonStyle}"
                            ToolTip="停靠到右侧"
                            Click="DockRight_Click">
                        <TextBlock Text="▶" FontSize="10"/>
                    </Button>
                    <Button Style="{StaticResource DockButtonStyle}"
                            ToolTip="停靠到底部"
                            Click="DockBottom_Click">
                        <TextBlock Text="▼" FontSize="10"/>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- 内容区域 -->
        <Border Grid.Row="1" 
                Background="White"
                AllowDrop="True"
                DragEnter="Content_DragEnter"
                Drop="Content_Drop">
            <ContentPresenter x:Name="ContentArea"/>
        </Border>
    </Grid>
</Window>
