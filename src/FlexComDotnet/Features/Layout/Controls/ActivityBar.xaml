<UserControl x:Class="FlexComDotnet.Features.Layout.Controls.ActivityBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="48"
             Background="#333333">
    
    <UserControl.Resources>
        <Style x:Key="ActivityBarButtonStyle" TargetType="ToggleButton">
            <Setter Property="Width" Value="48"/>
            <Setter Property="Height" Value="48"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                BorderThickness="2,0,0,0"
                                BorderBrush="Transparent">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Foreground" Value="White"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#007ACC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 主要按钮区域 -->
        <StackPanel x:Name="MainButtons" Grid.Row="0">
            <!-- 左侧面板切换 -->
            <ToggleButton x:Name="LeftPanelButton"
                          Style="{StaticResource ActivityBarButtonStyle}"
                          ToolTip="切换左侧面板"
                          Checked="LeftPanelButton_Checked"
                          Unchecked="LeftPanelButton_Unchecked"
                          IsChecked="True">
                <TextBlock Text="☰" FontFamily="Segoe UI Symbol"/>
            </ToggleButton>
            
            <!-- 右侧面板切换 -->
            <ToggleButton x:Name="RightPanelButton"
                          Style="{StaticResource ActivityBarButtonStyle}"
                          ToolTip="切换右侧面板"
                          Checked="RightPanelButton_Checked"
                          Unchecked="RightPanelButton_Unchecked"
                          IsChecked="True">
                <TextBlock Text="📋" FontFamily="Segoe UI Symbol"/>
            </ToggleButton>
            
            <!-- 底部面板切换 -->
            <ToggleButton x:Name="BottomPanelButton"
                          Style="{StaticResource ActivityBarButtonStyle}"
                          ToolTip="切换底部面板"
                          Checked="BottomPanelButton_Checked"
                          Unchecked="BottomPanelButton_Unchecked"
                          IsChecked="False">
                <TextBlock Text="▼" FontFamily="Segoe UI Symbol"/>
            </ToggleButton>
        </StackPanel>
        
        <!-- 底部设置区域 -->
        <StackPanel Grid.Row="1" VerticalAlignment="Bottom">
            <Button x:Name="SettingsButton"
                    Width="48" Height="48"
                    Background="Transparent"
                    BorderThickness="0"
                    Foreground="#CCCCCC"
                    FontSize="18"
                    Cursor="Hand"
                    ToolTip="设置"
                    Click="SettingsButton_Click">
                <TextBlock Text="⚙" FontFamily="Segoe UI Symbol"/>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
