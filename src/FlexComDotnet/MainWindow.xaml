<Window x:Class="FlexComDotnet.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FlexComDotnet"
        xmlns:serialViews="clr-namespace:FlexComDotnet.Features.Serial.Views"
        mc:Ignorable="d"
        Title="FlexCom - 串口调试助手" 
        Height="600" 
        Width="1000"
        MinHeight="500"
        MinWidth="800"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280" MinWidth="240" MaxWidth="350"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <!-- 左侧：串口配置面板 -->
        <Border Grid.Column="0" 
                Background="#FAFAFA"
                BorderBrush="#E0E0E0"
                BorderThickness="0,0,1,0">
            <serialViews:SerialConfigView x:Name="SerialConfigPanel"/>
        </Border>
        
        <!-- 分隔条 -->
        <GridSplitter Grid.Column="1" 
                      Width="4" 
                      HorizontalAlignment="Center"
                      Background="Transparent"
                      Cursor="SizeWE"/>
        
        <!-- 右侧：收发区域 -->
        <serialViews:SerialCommunicationView Grid.Column="2" 
                                              x:Name="SerialCommunicationPanel"/>
    </Grid>
</Window>
